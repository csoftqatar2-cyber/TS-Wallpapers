<?xml version="1.0" encoding="utf-8"?>
<!-- The activation card. Same warm card fill + hairline gold border as the rest of the app,
     so the one screen a customer sees before anything else reads as the same product. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="#2e2417" />
    <stroke android:width="1dp" android:color="@color/aurora_border" />
    <corners android:radius="20dp" />
</shape>
